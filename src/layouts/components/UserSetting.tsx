import {
  Box,
  Drawer,
  type DrawerProps,
  Flex,
  type Sx,
  Text,
  Modal,
} from "@mantine/core";
import { nanoid } from "nanoid";
import Link from "next/link";
import React from "react";

export const UserSetting: React.FC<DrawerProps> = ({ ...props }) => {
  return (
    <Modal {...props} title="Authentication">
      {/* Modal content */}dasdasdasdas
    </Modal>
  );
};
